# dotfiles

Personal Linux configuration (Hyprland-based).

## Structure
- `home/`   → files for `$HOME`
- `config/` → contents of `~/.config`

## Usage
Clone into `~/repos/dotfiles` and create symlinks manually.
